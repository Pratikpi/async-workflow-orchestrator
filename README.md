# async-workflow-orchestrator
A lightweight Python workflow orchestrator built with async I/O, thread-based workers, state machines, REST APIs, and SQLAlchemy persistence. Demonstrates modern backend architecture using asyncio, transitions, worker queues, and a relational database.
